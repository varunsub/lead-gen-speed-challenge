build:
  docker:
    backend: server/dockerfile
  scripts:
    postdeploy: ./create_gcp_service_key.sh

release:
  command:
    - echo "Release phase script running"

run:
  backend: uvicorn server.app.main:app --host 0.0.0.0 --port 8000